# FILE: user-service-prod.yml
# ENVIRONMENT: Production
# LOCATION: GitHub repository

spring:
  datasource:
    url: jdbc:mysql://prod-db-server:3306/user_service_db?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

server:
  port: 8001
  compression:
    enabled: true
    min-response-size: 1024

logging:
  level:
    root: WARN
    com.user: INFO
  file:
    name: /var/log/user-service/application.log
    max-size: 10MB
    max-history: 30
